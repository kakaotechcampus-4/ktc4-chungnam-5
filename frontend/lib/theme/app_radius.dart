import 'package:flutter/widgets.dart';

/// Figma `hOxrHBitBpjwIBBg2GO49y` node 96:16(식사 카드)에서 실측한 모서리 값.
class AppRadius {
  AppRadius._();

  static const double chip = 9;
  static const double thumbnail = 11;
  static const double card = 13;

  static const BorderRadius chipBorderRadius = BorderRadius.all(
    Radius.circular(chip),
  );
  static const BorderRadius thumbnailBorderRadius = BorderRadius.all(
    Radius.circular(thumbnail),
  );
  static const BorderRadius cardBorderRadius = BorderRadius.all(
    Radius.circular(card),
  );
}
